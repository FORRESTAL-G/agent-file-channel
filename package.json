{
  "name": "pi-agent-channel",
  "version": "1.0.0",
  "description": "Mailbox channel via filesystem for coordinating two pi agents (separate sessions) on a shared working tree. Blocking semaphore + readable local log. Ships a custom tool (agent_channel), a prompt, and the underlying node script.",
  "keywords": ["pi-package"],
  "license": "MIT",
  "bin": {
    "agent-channel": "./bin/agent-channel.js"
  },
  "pi": {
    "extensions": ["./extensions"],
    "prompts": ["./prompts"]
  }
}
